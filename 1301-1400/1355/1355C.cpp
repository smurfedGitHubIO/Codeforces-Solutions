#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;

int main(){
  ll a, b, c, d;
  cin >> a >> b >> c >> d;
  for(ll i=c; i<=d; i++){
    if(i-c > 0 and i-c < )
  }
  return 0;
}